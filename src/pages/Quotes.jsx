import { FileText } from 'lucide-react'; import Button from '../components/ui/Button.jsx'; import Card from '../components/ui/Card.jsx'; import { Empty } from '../components/ui/Form.jsx'; import { money } from '../utils/format.js'
export default function Quotes({quotes,convertQuoteToInvoice}){return <Card><h3>Quotes</h3>{quotes.length===0&&<Empty>Create a quote from the invoice designer.</Empty>}{quotes.map(q=><div className="list" key={q.id}><b>{q.quoteNo} <span className="chip chip-sent">{q.status}</span></b><span>{q.customerName} · {q.vehicle}</span><small>{q.quoteDate} · {money(q.total)}</small><Button variant="outline" onClick={()=>convertQuoteToInvoice(q)}><FileText size={16}/>Convert to Invoice</Button></div>)}</Card>}